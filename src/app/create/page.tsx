export default function CreatePage() {
  return (
    <main style={{ padding: 32, fontFamily: 'system-ui, sans-serif' }}>
      <h1>Create Studio Placeholder</h1>
      <p>Phase 0 route shell for domain selection, templates, references, dropdown controls, prompt preview, audio, and upscale.</p>
    </main>
  );
}
